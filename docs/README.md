# docs/

Place `.tex` (LaTeX) and `.typ` (Typst) source files here.

CI will automatically compile them to `src/uploads/<name>.pdf` on every push to `main`.

After compilation, add an entry to `src/_data/documents.json` to have the document appear on the summaries page.
